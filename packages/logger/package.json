{
  "name": "@showcase/logger",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "eslint \"src/**/*.ts*\"",
    "coverage": "jest --coverage",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/node": "^18.11.9",
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "jest": "^26.6.3",
    "jest-presets": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "^4.8.3"
  }
}
